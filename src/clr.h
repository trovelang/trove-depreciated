#pragma once

#include <log.h>
/*
#include <types.h>
#include <string_view>
#include <string>
#include <token.h>

class Clr {
public:
	enum Type : u8 {
		
	};

	Clr() {}
	Clr(Type type, SourcePosition source_position) : type(type), source_position(source_position) {}

	SourcePosition& get_position() {
		return source_position;
	}
	std::string to_string();

	std::string& get_value() {
		return value;
	}

	s32 as_num() {
		return atoi(value.c_str());
	}

	f32 as_float() {}

	Type& get_type() {
		return type;
	}

private:
	Type type;
	SourcePosition source_position;
	std::string value;
};

extern const char* token_type_debug[];
*/